#include "main.h"
#include <string.h>

/**
 * _strcmp - func to compare strings
 * @s1: first str
 * @s2: second str
 * Return: string
 */

int _strcmp(char *s1, char *s2)
{
	int res;

	res = strcmp(s1, s2);
	return (res);
}
